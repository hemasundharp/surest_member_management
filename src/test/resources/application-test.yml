spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/member_db_test  # Different DB for integration tests
    username: postgres
    password: sundharp
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: false
app:
  jwt:
    secret: TestJwtKeyForHMACSHA256_1234567890abcdefghijklmnopqrst

logging:
  level:
    org:
      springframework:
        security: DEBUG
